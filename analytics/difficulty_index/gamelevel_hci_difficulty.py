"""
    Each game level has a difficulty score based on
    human computer interaction.
"""


def get_hci_difficulty(game_level):
    """
    The HCI difficulty is assigned by the gamelevel creator which is
    stored in the database. We need to query the database and return
    the value?
    """
    dif_idx = 0

    return return dif_idx

